{
  "name": "floor-native",
  "version": "1.0.0",
  "description": "Capacitor Android shell for The Floor — loads the GitHub Pages site in a WebView so the app works while Chrome is blocked",
  "private": true,
  "scripts": {
    "build:apk": "node make-icons.mjs && capacitor-assets generate --android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "6.2.1",
    "@capacitor/cli": "6.2.1",
    "@capacitor/core": "6.2.1"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "sharp": "0.32.6"
  }
}
